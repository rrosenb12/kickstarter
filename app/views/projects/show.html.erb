<h1><%= @project.name%> </h1>
<h3>Description</h3>
<p><%= @project.description %></p>
<p><%= link_to("Edit Project", edit_project_path(@project.id))%></p>
<p><%= link_to("Projects Page", projects_path)%></p>
<p><%= button_to("Delete Project", project_path(@project.id), method: :delete)%></p>